// Gatsby supports TypeScript natively!
import React from "react"

import Layout from "../components/layout"
import SEO from "../components/seo"

const News = (props) => (
  <Layout>
    <SEO title="artebella news" />
    <h1>artebella news</h1>
  </Layout>
)

export default News
